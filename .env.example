# SQL Server SA password (must satisfy SQL Server complexity requirements)
MSSQL_SA_PASSWORD=YourStrong!Passw0rd

# Optional paths used by docker-compose.override.yml for local development
# Defaults point to project-local .docker folders when not set.
USER_SECRETS_PATH=$HOME/.microsoft/usersecrets
HTTPS_CERTS_PATH=$HOME/.aspnet/https
